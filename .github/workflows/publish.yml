name: Publish 1.8.9

on:
  push:
  pull_request:
  workflow_dispatch:

env:
  LOADER: forge
  JAVA_VERSION: 8
  MINECRAFT_VERSION: 1.8.9
  VERSION_CORE: 0.0.1
  VERSION_TYPE: alpha

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check Environment Variables
        run: env

      - name: Checkout Repository
        uses: actions/checkout@v3
        with:
          submodules: true

      - name: Setup Java
        uses: actions/setup-java@v2
        with:
          distribution: "temurin"
          java-version: 17

      - name: Make Gradle Wrapper Executable
        if: ${{ runner.os != 'Windows' }}
        run: chmod +x ./gradlew

      - name: Build
        run: ./gradlew clean build

      - name: Debug
        run: |
          cd build
          ls
          cd libs
          ls

      - name: Generate Metadata
        run: |
          echo "VERSION=${{ env.VERSION_CORE }}-${{ env.VERSION_TYPE }}+${{ env.MINECRAFT_VERSION }}" >> $GITHUB_ENV
          echo "NAME=NotifHy ${{ env.VERSION }}" >> $GITHUB_ENV

      - name: Publish
        uses: Kir-Antipov/mc-publish@v3.2
        env:
          FILE: /build/libs/*
        with:
          github-tag: "v${{ env.VERSION }}"
          github-token: "${{ secrets.GITHUB_TOKEN }}"

          name: "${{ env.NAME }}"
          version: "${{ env.VERSION }}"

          loaders: "${{ env.LOADER }}"
          game-versions: "${{ env.MINECRAFT_VERSION }}"
          java: "${{ env.JAVA_VERSION }}"

          files: "${{ env.FILE }}"